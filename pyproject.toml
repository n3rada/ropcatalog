[tool.poetry]
name = "rp-catalog"
version = "1.0.0"
description = "Finding and classifying gadgets from rp++ output file."
authors = ["n3rada <72791564+n3rada@users.noreply.github.com>"]
license = "GPL-3.0-only"
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.9"
prompt-toolkit = "^3.0.48"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"


[tool.poetry.scripts]
rp_catalog = "rp_catalog.console:run"